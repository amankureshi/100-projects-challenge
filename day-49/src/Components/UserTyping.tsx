const UserTyping = () => ({
    userInput,
    className,
}: {
    userInput: string;
    className?: string;
}) => {
    const typedCharacters = userInput.split("")
    return (
        <div>

        </div>
    )
}

export default UserTyping
